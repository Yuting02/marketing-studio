# 技术债务追踪

> “已有但做得不好”的部分。待开发功能在 `backlog.md`。
> 每条格式：`[优先级 高/中/低] 问题 — 影响`

## 当前债务
- [中] 提示词双份维护 — `docs/prompts.md` 与 `api/*.js` 里的 prompt 字符串是两份，靠人工保持一致；不同步就会“文档 ≠ 实际”（本次刚修过一次 review 节点的此类漂移）。可考虑让代码从单一来源读取。
- [中] `src/App.jsx` 过大（约 700 行）— 主组件、所有展示子组件、请求逻辑都堆在一个文件，难维护。可把子组件拆到单独文件。
- [中] 零自动化测试 — 没有任何 `*.test.*`。`buildVariantReview`（纯函数）最该先补单测。
- [低] `bannedWords` 仅英文 — 法语广告的绝对化词漏检（也在 `backlog.md` 里作为功能项）。
- [低] `.gitignore` 有冗余 / 可能自相矛盾 — 末尾的 `.env*` 会盖掉前面 `!.env.example` 的负向规则，且与 `.env.*` 重复。建议清理。

## 已解决
- [✓ 2026-06-28] review 节点 `prompts.md` 与代码不一致（中文 7 条 vs 英文 4 条）— 已统一为中文 7 条，并补回防中英混用的规则。
